import Navbar from "../smallComponents/Navbar";

const ViewArticle = (props) => {
    const {userId} = props;

    return (
        <div>
            
        </div>
    )
}

export default ViewArticle;